<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>No Listings Found</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    body {
      background-color: #f8f9fa;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
    }

    .error-box {
      text-align: center;
      padding: 30px;
      background: white;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      animation: slideIn 0.6s ease-out;
    }

    .error-title {
      font-size: 2rem;
      font-weight: 600;
      color: #dc3545;
    }

    .error-message {
      margin-top: 10px;
      font-size: 1.2rem;
      color: #6c757d;
    }

    .btn-back {
      margin-top: 20px;
      padding: 10px 20px;
      border-radius: 25px;
    }

    @keyframes slideIn {
      0% {
        transform: translateY(-40px);
        opacity: 0;
      }
      100% {
        transform: translateY(0);
        opacity: 1;
      }
    }
  </style>
</head>
<body>
  <div class="error-box">
    <h1 class="error-title">No Listings Found</h1>
    <p class="error-message">We couldn’t find any listings in the selected category. Try choosing a different one.</p>
    <a href="/listings" class="btn btn-outline-primary btn-back">Back to Listings</a>
  </div>
</body>
</html>
